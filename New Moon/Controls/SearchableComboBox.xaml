<UserControl x:Class="P3FESTrainer.Controls.SearchableComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="18" />
        </Grid.ColumnDefinitions>
        <TextBox x:Name="EditBox" Grid.Column="0"
                 IsReadOnly="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ReadOnly}"
                 TextChanged="EditBox_TextChanged"
                 PreviewKeyDown="EditBox_PreviewKeyDown"
                 GotFocus="EditBox_GotFocus"
                 LostFocus="EditBox_LostFocus"
                 Padding="2,2,22,2" />
        <!-- Dropdown arrow button -->
        <Border Grid.Column="0" HorizontalAlignment="Right" Width="20" Margin="0,1,1,1"
                Background="#E8E8E8" BorderBrush="#888" BorderThickness="1">
            <ToggleButton x:Name="DropdownButton" Background="Transparent" BorderThickness="0"
                          Focusable="False"
                          IsChecked="{Binding IsOpen, ElementName=ResultsPopup, Mode=OneWay}"
                          Click="DropdownButton_Click">
                <Path Data="M 0 0 L 5 5 L 10 0 Z" Fill="#333" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </ToggleButton>
        </Border>
        <Popup x:Name="ResultsPopup" PlacementTarget="{Binding ElementName=EditBox}"
               StaysOpen="False" AllowsTransparency="True" Width="{Binding ElementName=EditBox, Path=ActualWidth}">
            <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                <ListBox x:Name="ResultsList" MaxHeight="220"
                         PreviewMouseUp="ResultsList_PreviewMouseUp"
                         KeyDown="ResultsList_KeyDown" />
            </Border>
        </Popup>
    </Grid>
</UserControl>
